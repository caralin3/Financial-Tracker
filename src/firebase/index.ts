import * as auth from './auth';
import * as db from './firebase';
import * as firebase from './firebase';
import { currentUser, usersCollection } from './firebase';

export {
  auth,
  currentUser,
  db,
  firebase,
  usersCollection,
};