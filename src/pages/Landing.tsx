import * as React from 'react';
import { SignUpForm } from '../components';

export const LandingPage: React.SFC = ({history}: any) => (
  <div>
    <h1>Landing Page</h1>
    <SignUpForm />
  </div>
)
